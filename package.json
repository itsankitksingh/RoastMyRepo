{
  "name": "gitroast",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/api ci --include=dev && npm --prefix apps/api run build && npm --prefix apps/web ci --include=dev && npm --prefix apps/web run build",
    "start": "npm --prefix apps/api run start:prod"
  }
}
